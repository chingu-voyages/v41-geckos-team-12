{
  "name": "@geckochat/server",
  "version": "1.0.0",
  "description": "geckos chat app",  
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon server.js",
    "lint": "npx eslint .",
    "lint:fix": "npx eslint . --fix",
    "pre-commit": "npx lint-staged"
  },
  "lint-staged": {
    "**/*.js": [
      "prettier --write .",
      "eslint . --fix"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^4.5.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  },
  "private": true,
  "type": "module"
}
